# Versions this work was run with (Python 3.12).
# Pinned because the saved model is a pickle and is tied to the scikit-learn version.
numpy==2.5.1
pandas==3.0.5
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
sentence-transformers==5.6.1
transformers==5.14.1
torch==2.13.0
nltk==3.10.0
iterative-stratification==0.1.9
matplotlib==3.11.1
openpyxl==3.1.5

# Only needed to redo verb extraction from the source documents (BT 2).
stanza==1.12.0
